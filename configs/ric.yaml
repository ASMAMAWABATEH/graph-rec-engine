model: ric
top_k: 10

# RIC model controls
decay: 0.7
alpha: 0.5
beta: 0.3
gamma: 0.2

# Optional edge-time decay in Neo4j edge loading (0.0 disables)
edge_time_decay: 0.0

# Hybrid scoring mix: 0.0 raw weight, 1.0 probability, 0.5 blend
transition_mix: 0.5
